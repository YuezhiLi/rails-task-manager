<h2><%= @task.id %>. <%= @task.title %></h2>
<p><%=  @task.details %></p>
<p><%= @task.completed ? "completed" : "not completed" %></p>
<p><%= link_to "Edit task", task_edit_path(:id => @task.id) %></p>
<p></p>
<h2><%= link_to "Back to list", tasks_path %></h2>
